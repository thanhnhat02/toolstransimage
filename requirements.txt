# ─────────────────────────────────────────────────────────────────────────────
# AI Image Enhancer — requirements.txt
# Ubuntu 24.04 / Python 3.12
# ─────────────────────────────────────────────────────────────────────────────

# ── GUI ───────────────────────────────────────────────────────────────────── #
PySide6>=6.7.0

# ── Core image processing ─────────────────────────────────────────────────── #
Pillow>=10.3.0
opencv-python-headless>=4.9.0
numpy>=1.26.0
scipy>=1.13.0

# ── Deep Learning framework ───────────────────────────────────────────────── #
# GPU version (CUDA 12.1) - most common for Ubuntu 24.04 + NVIDIA
# Install separately if needed:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.3.0
torchvision>=0.18.0

# ── Real-ESRGAN ───────────────────────────────────────────────────────────── #
basicsr>=1.4.2
facexlib>=0.3.0
realesrgan>=0.3.0

# ── Face Restoration ─────────────────────────────────────────────────────── #
gfpgan>=1.3.8

# ── Diffusers ecosystem (SDXL, ControlNet, Flux) ─────────────────────────── #
diffusers>=0.27.0
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.3

# Optional: memory-efficient attention (Volta+ GPUs only)
# xformers>=0.0.26

# ── ControlNet auxiliary models ───────────────────────────────────────────── #
controlnet-aux>=0.0.7

# ── Utilities ─────────────────────────────────────────────────────────────── #
tqdm>=4.66.0
requests>=2.31.0
packaging>=24.0
huggingface_hub>=0.23.0
einops>=0.8.0
timm>=1.0.3

# ── Build / packaging (optional, for PyInstaller) ─────────────────────────── #
pyinstaller>=6.6.0
